# assets

Put images here.

- **mascot.png** — (optional) Add your cloud mascot image with this exact
  filename and it will automatically appear in the hero section, replacing the
  built-in CSS/SVG placeholder.

This folder is kept in the repo so GitHub Pages always has a valid `assets/`
path. You can delete this file once you add real assets.
